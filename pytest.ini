[pytest]
testpaths = tests
markers =
    unit: Pure unit tests with no external dependencies
    component: Component tests using FastAPI TestClient with in-memory SQLite
    integration: Integration tests verifying direct database interactions
    e2e: End-to-end workflow tests covering complete user journeys
addopts = --strict-markers
